# Monorepo Environment Notes
#
# This repository keeps runtime env files per app package:
# - Backend: copy `backend/.env.example` to `backend/.env`
# - Frontend: copy `frontend/.env.example` to `frontend/.env`
#
# Root-level scripts that start Docker use `backend/.env` as their env source..
